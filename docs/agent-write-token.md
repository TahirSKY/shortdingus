# Agent write token

The token for `asset-ingest` lives only in Lovable Cloud secrets, under the name `HUB_WRITE_TOKEN`.
Never commit its value to this repo or paste it into any file. Give it to trusted agents directly.
If it leaks, rotate the secret.
